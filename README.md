# Github API

This application is for fetching the `github user details` by entering their `Username` as search criteria.
This application uses `Github API` for fetching the results.
This application is done using `ReactJS`.
